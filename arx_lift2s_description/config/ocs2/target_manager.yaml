/**:
  ros__parameters:

    # 是否启用VR输入处理
    enable_vr: true

    # RViz Fixed Frame on Lift2S. Split control_base_frame comes from acone
    # task.info (arm_base). Marker TF base_link←arm_base includes real lift height.
    marker_fixed_frame: base_link
