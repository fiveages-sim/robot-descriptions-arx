/**:
  ros__parameters:
    linear_scale: 0.3
    angular_scale: 0.5
    enable_vr: true
    enable_head_control: false

    # X7S URDF root is base_link (no base_footprint). Override arms_target_manager default.
    marker_fixed_frame: base_link
    vr_follow_frame: base_link
